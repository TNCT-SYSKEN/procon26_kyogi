#include<iostream>
#include<fstream>
#include<vector>
#include<string>
#include<math.h>
#include"algorithm.h"
using namespace std;

int main(){
	algorithm p;

  p.input_file();
	
	p.output_file();

  //ドラッグ＆ドロップ用
  /*ifstream ifs(argv[1], 1);
  if (ifs.bad())return -1;
  string line;
  */
}
